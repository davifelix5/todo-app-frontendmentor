import styled from 'styled-components';

export const Input = styled.input`
  color: ${({theme}) => theme.fontColorLigth};
  padding: 1rem;
  border-radius: 8px;
  width: 100%;
  height: 100%;
  background: none;
  border: none;
`;